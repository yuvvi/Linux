1. RSYNC copy

    $rsync -rP -u -z <source path >   <Destination path>

2. SCP copy

    $scp source username@destinationIP:/home/
    
3. filter similar lines in a file
    $cat 1_txt_CSV.txt | awk -F " " '{print $9}' | sed -e "s/[0-9]//g" | sort -u > txt_different_file.txt
    
